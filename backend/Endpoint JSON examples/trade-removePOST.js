
/**
 * You will need to provide the following in the request body:
 *      tid - the id of the trade to be deleted
 * 
 * Sample JSON output after removing the specified trade object:
 * trading - the updated array of trade objects that is returned after the request is successful
 */
const example_response = {
    "trading": [
        // Remaining trade objects after the specified trade is deleted
    ]
}
